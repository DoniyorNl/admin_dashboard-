# API Configuration
# Local development default: http://localhost:4000
NEXT_PUBLIC_API_URL=http://localhost:4000

# Auth API Configuration  
# Local development default: http://localhost:4001
NEXT_PUBLIC_AUTH_API_URL=http://localhost:4001

# Environment
NODE_ENV=development

# Email Configuration (Gmail SMTP)
# Gmail address for sending emails
SMTP_USER=your-email@gmail.com

# Gmail App Password (NOT your regular Gmail password!)
# How to get: https://myaccount.google.com/apppasswords
SMTP_PASSWORD=your-app-password-here

# SMTP Settings (Gmail defaults)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# From name that appears in emails
SMTP_FROM_NAME=Admin Dashboard

# Abstract API for Email Verification (Optional)
# Get free API key: https://app.abstractapi.com/api/email-validation/
# Free tier: 100 requests/month
ABSTRACT_API_KEY=your-abstract-api-key-here
